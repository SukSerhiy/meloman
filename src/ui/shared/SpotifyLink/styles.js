import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  spotifyLogo: {
    width: 20,
    marginLeft: 20,
    cursor: 'pointer',
  },
}))

export default useStyles
